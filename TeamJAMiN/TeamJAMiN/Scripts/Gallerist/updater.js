//JS File for updating html elements

$(function (elementIdentifier, newElement) {    //identifier is element name, newElement is the string replacing info
    var element = document.getElementById(elementIdentifier);
    element = newElement;
});